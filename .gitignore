# Smithy build output (regenerated by `smithy build`)
build/

# Smithy CLI cache
.smithy/

# Node
node_modules/

# TypeScript compiled output (services build via esbuild in Docker / tsc locally)
dist/

# TypeScript incremental build info
*.tsbuildinfo

# Environment variable files (keep .env.example, ignore real secrets)
.env
.env.local
.env.*.local

# macOS
.DS_Store

# Editor
.vscode/
.idea/
