# Copy to .env in each service folder (or source from project root).
PORT=3000
RABBITMQ_URL=amqp://guest:guest@localhost:5672

# Postgres (used by payment-service only)
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=postgres
PGDATABASE=payments
